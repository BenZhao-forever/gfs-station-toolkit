# 核心依赖（同时兼容 Windows 7 的 Python 3.8 与新系统的 3.10+）
Flask>=3.0,<3.1
Werkzeug>=3.0,<3.1
requests>=2.28
pyserial>=3.5
pycryptodome>=3.15
# 可选（run.bat 会尽力安装，装不上自动降级）：
#   cryptography —— 本地密码加密（缺失则明文存本机）
#   ddddocr      —— 验证码本地识别，实现打印 token 全自动登录（缺失则后台手动输码）
